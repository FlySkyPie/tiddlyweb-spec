name: limit
in: query
description: |
  Limit the number of items returned. Syntax: `<count>` or `<index>,<count>`.
schema:
  type: string
  pattern: ^\d+(,\d+)?$
  Examples:
    - limit=10
    - limit=0,10
