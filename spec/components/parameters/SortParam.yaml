name: sort
in: query
description: |
  Sort the collection by an attribute. Prefix with `-` for descending order.
schema:
  type: string
  pattern: ^-?\w+$
  examples:
    - sort=modified
    - sort=-created
    - sort=title
